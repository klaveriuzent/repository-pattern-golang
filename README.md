# repository-pattern-golang

need `.env` file setup like
```
# Database credentials postgre
DB_HOST="set_credentials"
DB_USER="set_credentials"
DB_PASSWORD="set_credentials"
DB_NAME="set_credentials"
DB_PORT="set_credentials"
DB_DRIVER="postgres" #You can use: sqlsvr | mysql | postgres

SERVICE_NAME="repositoryPattern"
# Authentication credentials
TOKEN_TTL="set_credentials"
JWT_PRIVATE_KEY="set_credentials"
API_SECRET="set_credentials"
TOKEN_HOUR_LIFESPAN="set_credentials"
```
